# NPM configuration (project level)
# Visit: https://docs.npmjs.com/cli/using-npm/config

# Dependencies saved to package.json will be configured with an exact version rather than using npm's default semver
# range operator.
save-exact=true

# When "true" displays the message at the end of each npm install acknowledging the number of dependencies looking
# for funding.
fund=false